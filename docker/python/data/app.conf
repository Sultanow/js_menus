server {
    listen 80;
    client_max_body_size 50M;
    root /usr/share/nginx/html;
    location / { try_files $uri @app; }
    location @app {
        include uwsgi_params;
        uwsgi_pass python-service:5000;
    }
}
